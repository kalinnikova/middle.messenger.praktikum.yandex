{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "clear": "rm -rf ./dist",
    "start": "npm run clear && parcel src/*.html --open --no-cache",
    "build": "npm run clear && parcel build src/*.html --no-cache",
    "build:dev": "npm run build && serve ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "less": "^4.1.1",
    "parcel-bundler": "^1.12.5",
    "parcel-plugin-handlebars": "^0.4.4",
    "serve": "^12.0.0"
  },
  "dependencies": {
    "handlebars": "^4.7.7"
  }
}
